{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "debug-integraboot-qemu",
            "type": "shell",
            "command": "${workspaceFolder}/tools/debug-integraboot-qemu.sh",
            "dependsOn": [ "make-debug" ]
        },
        {
            "label": "make-debug",
            "type": "shell",
            "command": "cd ${workspaceFolder} && make debug",
        }
    ]
}